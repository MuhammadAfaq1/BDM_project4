# Secrets — copy to .env and fill in (never commit .env)
SLACK_WEBHOOK_URL=

# Optional overrides (defaults match docker-compose service names)
# POSTGRES_DSN=postgresql://rico:rico@postgres:5432/rico
# MINIO_ENDPOINT=http://minio:9000
# OLLAMA_URL=http://host.docker.internal:11434
